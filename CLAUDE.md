## Deployment rules
- Never run `vercel`, `vercel deploy`, or `vercel link`. Deploys happen automatically when we push to GitHub.
- The only Vercel project is `pawpredict-ai`. Never create a new one.
- To deploy: commit and `git push`. That's it.
